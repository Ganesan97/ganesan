#include<stdio.h>
#include <stdlib.h>

int main()
{
    int n,rem,digit,odd=0;
    scanf("%d",&n);
    while(n>0)
    {
        digit=n%10;
        n=n/10;
        rem=digit%2;
        if(rem !=0)
        
            odd=odd+digit;
        
        
    }
printf("%d",odd);

}
